SELECT 
  (D.DEPARTMENT+'/'+V.UNIT+'/'+[EMPLOYEE] ) as EMPLOYEE
  FROM [hr].[dbo].[HR_EMPLOYEE] H
  INNER JOIN VN_UNIT V
  ON H.UNIT_ID = V.ID
  INNER JOIN VN_DEPARTMENT D
  ON H.DEPARTMENT_ID = D.ID
where [EMPLOYEENO]='<EMPLOYEENO/>'